# Twenty-message history windows — source candidate, execution blocked

Confirmed 25 September user contract: initially about 20 newest user/assistant messages, excluding tools; scroll up for older pages; reuse device cache. This supersedes the 5 MB/2.5 MB proposal. Never delete native transcripts, model context or Vault. “localstorage” is a behavior request; device data stays in the existing encrypted IndexedDB, not plaintext localStorage.

Base `6f6fe30a08f3e763d74eed440824ffa84f831aa9`; continuation of `6e30bc9`, branch `feat/history-window-cache`, worktree `/root/WORKTREES/cr-history-window-cache`. No deployment or production mutation. Do not integrate until the checks below execute successfully and leader reviews the built delta.

## Read boundary and native compatibility

Saved generated protocol `/tmp/codex-orchestration-protocol/v2/{ThreadReadParams,ThreadItemsListParams,ThreadTurnsListParams,ThreadTimelineListParams,Thread,ThreadItem}.ts` was inspected. `Thread.path` supplies the persisted rollout path; `thread/read includeTurns:false` and `thread/turns/list itemsView:notLoaded,limit:1` supply bounded metadata. The item/timeline APIs have item-count pagination but no content byte limit in those schemas. They cannot safely retrieve an arbitrarily large item before clipping. Fresh installed-binary schema generation remains an executable acceptance gate, not claimed evidence.

`/history` now issues **two metadata RPCs per page**, zero `thread/items/list`, zero full `thread/read`; detail issues one metadata RPC. The latest native turn status is authoritative even when its JSONL completion record has not flushed. Startup/resume, selected-view consistency and unread reconciliation can each request pages; do not equate two RPCs per page with two total RPCs per browser open. Resume retains `excludeTurns:true`. Background Vault completion repair still has its pre-existing full hydration path; this patch does not claim to eliminate every internal full read.

Content comes from a new private read-only incremental index of trusted native `sessions`/`archived_sessions` JSONL. No user-provided paths; authorize thread/workspace first, resolve under native storage, open nonblocking/O_NOFOLLOW, verify regular FD canonical path, then verify session_meta thread ID/cwd. Recheck descriptor/current path identity and liveness after reads. Cursor contains HMAC-bound thread, generation and positions, never a filesystem path.

Parser consumes fixed 64 KiB blocks; it does not collect a raw JSONL line. Normal string prefixes retain at most16 KiB, aggregate normal strings64 KiB, containers64 entries/2048 nodes/depth64; identity fields are separately bounded. The unretained string tail still checks delimiters/escapes/control bytes using bounded block scanning. Known messages project to valid text/content objects; tools project to typed summaries, preserving IDs rather than recursively replacing fields with null. Unknown tool structures remain available as original record ranges. Internal reasoning/system/developer records are not expanded into the transcript.

Real-schema-shaped disposable fixtures cover session_meta/turn_context/task_started/task_complete/turn_aborted/item_completed and legacy response_item. Canonical/fallback message pairing is one-to-one on exact ID or untruncated normalized text, never suppression of all fallback messages within a turn. The native persistent JSONL format is a compatibility dependency, not a documented public pagination protocol: compare installed native fixture/schema/parity before rollout. Long clipped legacy messages lacking stable IDs cannot be safely text-paired; they remain visible rather than guessed away. No raw production conversation is needed for acceptance.

## Bounded work, cache and detail

- A page selects20 message records, at most60 associated tool summaries, and a counted tool-range entry when additional tools exist. Older cursor cuts at message boundaries without losing the intervening tool range. Partial turns retain turn IDs/status. Each original authorized tool record remains retrievable.
- Detail reads at most32,768 UTF-8 bytes directly from the verified FD; next/back use actual byte offsets, trim incomplete UTF-8 suffixes and keep128 back positions. No repeated native whole-item fetch. Updated single-record positions and rewritten-generation cursors fail visibly.
- Node22 built-in SQLite stores bounded projections in a private0700 directory/0600 files, 2 MiB SQLite page cache, maximum128 MiB per thread DB, one active index job and at most16 queued jobs. All SQL result arrays are limited to a page; checkpoint transactions occur at complete-record boundaries after about1 MiB. A single huge incomplete record is reparsed after cancellation; no mid-record checkpoint claims.
- Cold index still scans the complete source once, with bounded working memory. Warm unchanged open reads small head/tail guards (up to8 KiB) and queries the index. Append scans only the new suffix (plus an incomplete tail); persistent checkpoints survive restart. Truncation, inode replacement, same-size mtime change and head/tail mismatch rebuild a new generation. A concurrent append may be picked up next revalidation. This is cooperative consistency, not detection of malicious-root in-place rewrite+append preserving identity/anchors. Invalid/corrupt cache or exhausted disk cap fails visibly; no native-state restore. Per-thread disk cap does not constitute a global cache-disk quota.
- Cold opening can also trigger existing unread-gap reconciliation of other conversations. Those reads are now metadata/index reads, not full native hydration, but first-time index construction and unread-gap work still cost disk/CPU. Measurements must report actual total requests and index bytes, not just HTTP body size.
- Fresh owner/session proof precedes cached plaintext reuse on each unlock. Warm device cache paints before history network revalidation; unchanged encrypted responses use existing revision/unchanged protocol; append sends only a bounded current window. This is bounded-window replacement, not a bespoke per-item patch protocol. Already-unlocked offline browsing reuses cached pages. Cold offline unlock is intentionally not enabled.
- Upward wheel/touch/keyboard at the boundary retrieves one page; there is no automatic older-page drain. Stable item anchors preserve reading position; DOM caps240 items. Paused readers retain their window while latest cache updates. Native rewrite invalidates old cursors without moving the reader; explicit latest reload remains available. Pending/Plan/drafts/live authority remain separate. Lock/switch aborts or discards stale work.

## Acceptance authored, NOT executed

`server/history-json.test.ts`: small JSON/Unicode/escapes/prototype keys; huge strings/arrays; bounded block/byte-parser equivalence; invalid tails/depth/keys. `server/history-pages.test.ts`:20 excluding tools, every tool detail accessible, restart/no-change/append counters, incomplete line, signed/cross-thread/stale cursors, native source unchanged, synthetic16 MiB single record,32 KiB details, UTF-8, cancellation, parent replacement/truncation and typed render structures. Controller tests require two metadata RPCs/page and original unread boundary. Client tests cover bounded deduplication and paused/rewrite state.

`scripts/history-window-browser.mjs` writes a real disposable **~197 MB whole JSONL transcript across1000 tools**. 197 MB is the earlier whole Chung transcript size, not a proven single-item size; the16 MiB single-item test is explicitly synthetic. Browser fixture covers1280×900/390×600, encrypted cold/warm/unchanged, blocked-network warm paint, offline older cache, append while closed, upward anchor/no drain, live-event reading position, Plan/draft, delayed switch/Lock, cookie-only denial and no plaintext localStorage. It records actual RPC counts/native response bytes/completed encrypted wire bytes/DOM/RSS/time. Shared native/browser fixtures now provide owned persisted logs and bounded turn metadata. Existing secure browser fixture supplies additional replay/reconnect/lifecycle controls.

No new benchmark result or screenshot exists yet. Historical `d43da8f` measurements apply to the previous prototype only. No pass counts are claimed for these tests.

## Exact execution blockers and next command

Current workspace-write/approval-never/network-restricted environment is unchanged. Previous attempt recorded `codex-heavy` failing BEFORE execution with EROFS creating `/var/log/codex-heavy/...service.json`; Git push failed resolving github.com; checked knowledge API failed EPERM to127.0.0.1:5174. Do not bypass the required runner or overwrite maintained Vault files. This continuation does not waste retries under identical permissions.

Once the execution environment permits the required runner, run sequentially from this worktree:

```
codex-heavy --label history-window-check --timeout 1800 -- bash -lc 'set -e; export NODE_OPTIONS=--max-old-space-size=1024 VITEST_MAX_WORKERS=1 PLAYWRIGHT_MODULE=/tmp/working-hours-browser/node_modules/playwright/index.mjs; npm run check; node scripts/history-window-browser.mjs; node scripts/plan-questions-browser.mjs; node scripts/secure-api-browser.mjs; node scripts/contextual-push-browser.mjs; node scripts/team-panel-browser.mjs; node scripts/owner-files-browser.mjs'
```

Collect metrics/screenshots, fix any failure, confirm native persisted-format parity with fake/native schema fixtures, then build artifact manifest and NEW-only runner for leader review. Source implementation and test design are present; executable validation, installed-native parity, push and rollout preparation remain gated. No service availability claim.
