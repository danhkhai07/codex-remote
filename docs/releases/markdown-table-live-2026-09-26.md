# Markdown table wrapping — NEW frontend LIVE

Task `f7be587e-fc3b-4a37-a3b4-75d04b1789e8` published accepted product source
`4ee45dbd559c9c975a0c26fb1fb2d2af3d3920a0` over the verified LIVE source
`d6990d30b98ec4100e26752213017a90c0559853`. The publication was frontend-only;
the NEW service was not restarted and OLD remained disabled/inactive.

The initial guarded apply stopped before its first write because the unchanged
`docx-preview` JavaScript reused an immutable map filename whose build source
locations differed. The two maps had identical version, file, names,
sourcesContent, ignore list and mappings, with matching source basenames. The
package retained the already-LIVE map byte and recorded that normalization in
`map-normalization.json`. The controlled continuation was allowed only because
`publish-failure.json` proved zero writes and the original verified backup and
entire runtime baseline still matched.

Ten files were written: nine new hashed CSS/JavaScript/map assets, followed by
`index.html` last. `sw.js` and the other 15 graph files already matched and were
skipped. All earlier hashed assets remain for existing tabs. The shared
publication lock guarded every destination/preimage and was released normally.

Postverification fetched all 25 candidate files from loopback and public
`https://remote.danhkhai.io.vn` and matched exact hashes. `/`, `/files`, local
and public health passed. PID `2937958` and process start identity were unchanged;
the backend tree, helpers, dependencies, config/key identities and Hours
JavaScript/map remained unchanged. Hours hashes remain
`b763a0f7b74c0341684e196855e85b3f5e3b123915a373b27463c17916702e93`
and `6a76da381331d7a802da5d844d341da742f0bfc3809f0cfe3b74370d55c42fb8`.

The existing `/services` root entry was updated through the encrypted NEW CLI,
preserving TECH/tool/history/cache/Push/Files/Hours features and adding Markdown
table wrapping. No Files or Hours service entry was created. The focused fake
browser evidence remains the accepted UI check at 1280/768/390/320; production
verification was read-only HTTP/encrypted maintenance and did not open or
mutate a real transcript. Physical Safari/iOS remains untested.

Private release and evidence:
`/root/.local/state/codex-remote-secure/releases/markdown-table-4ee45db`.
`LIVE.json` SHA256 is
`db93a5aa145a312e6723875c5e2c6362a19f69610c1c8be2051df7d2e60ea011`;
normalized candidate SHA256 is
`8e6bb415b05ee7b63360deb2bf8358f1c50ba35c11c51a6d944ec7380a1604f1`;
published receipt SHA256 is
`fa185dfdc2f05070753f89fbb1f468bab09471e7e614a906a56696f053b99a6d`.
