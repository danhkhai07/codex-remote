# TECH recovery and tool disclosure — integration candidate

Task f5ed05cf-9008-4bad-95ea-e61d402a75c4. Base LIVE
be0dcbc4093ad516b10df4452f2ab86d06f1f700, NEW service only. No impeccable,
agent delegation, real model turn or production mutation. Actual receipt:
gpt-6-astra/xhigh/FullAccess.

## Accepted inputs and product merge

Product merge fee47b5 preserves both accepted source lines:

- b7022141908face280f80a52fdf5816542d52a8a: malformed complete native paginated
  JSONL recovery after identity validation, no transcript edits or ordinal
  invention. Product modules history-json and rollout-history only.
- db113c7b40330146a150ac7adacd8205f54b57e6: lazy tool disclosure, clipped-message
  detail access and backward retry operation preservation. This includes the
  U1/U2 corrections; c3ea372 alone is not accepted. Product App/HistoryDetail only.

Root decisions are tech-source-review.json and tool-rereview.json under
/root/.local/state/codex-remote-secure/reviews/history-postlive-20260925.
The browser merge retains malformed-source data and all tool/U1/U2 controls.
No accepted product behavior was rewritten during integration.

## Executed checks

Sequential codex-heavy full npm run check:634 Vitest tests/90files,11 Node
readiness checks, lint/typecheck/client/server build/PWA passed. The combined
real encrypted HTTP browser fixture passed1280×900 and390×600:20 messages,
malformed native source, zero collapsed detail requests, lazy user/assistant/tool
content,32KiB chunks, collapse/cache/error/repeated backward Retry, H1 frozen
history/latest control, deep240 eviction, anchor/no-drain/draft/SSE/switch/Lock.
No physical Safari/iOS or production model turn is claimed.

Runner checks:16 owned controls exercise the actual history runner modules,
including own queued work, shared-lock baseline, late work before publication or
restart, partial/ambiguous failure without automatic rollback/restart retry,
orchestration dispatch/report guards and the four-file allowlist. Seven private
postverify controls verify bounded JSON/messages/IDs/errors without production
access. No additional full app run is needed after packaging-only changes.

The b702214 installed-native oracle and real TECH reader evidence were reused
by15 receipt hashes, not re-executed:8 fake native0.155 comparisons/75RPC;
actual TECH309messages/16pages, cold1.92s/warm8ms/older6ms/restart7ms, source
identity/size/mtime unchanged. Those are local reader timings, not browser or
public API timings. Prior197MB cache/Plan/offline acceptance also remains in those
hash-bound receipts. See docs/performance/history-tech-general-2026-09-25.md.

The client is freshly rebuilt from the accepted source. Vite intentionally
changes its service-worker build ID; resulting hashed chunk names and source-map
relative locations differ from CR4's external-output build. A bounded comparison
checks every client byte after normalizing only these three build-location/ID
fields; shipped bytes are never transformed. All four backend files must remain
byte-identical to b702214. Product source equality is separately checked.

## Package and activation boundary

Private preparation/evidence:
/root/.local/state/codex-remote-secure/history-postlive-f5ed05cf.
New release:
/root/.local/state/codex-remote-secure/releases/history-window-postlive-f5ed05cf.
The private delivery.json/README.md contain the exact final commit, manifest,
payload/preimage/seal hashes, helper hashes and commands. This document does not
claim activation; root must review that concrete final package before arming.

Payload: exactly history-json.js/map + rollout-history.js/map, plus the complete
25-file client. No full dist-server copy. Known secure-client.js/map and
event-hub.js.map source/build differences stay excluded with exact pair guards.
Hours JS b763a0f7 and map6a76da38, both templates703ad317, generator, key identity,
environment, dependencies, native/Vault/Hours data and disabled OLD remain.

Runner derives from the reviewed H1 release. core.mjs/readiness.mjs are unchanged.
Only the pinned LIVE baseline and four-file deployment allowlist change in the
runner; artifacts.mjs uses that same source/receipt. Immutable baseline/seal must
be recreated on drift, never edited. Frozen maintenance closure still targets
NEW5174, including all active turns, queued/native-unsettled jobs, reports and
pending requests. No own-task exemption. Dual idle checks5seconds apart and
immediate rechecks precede copy and a single ordinary NEW restart. Old frontend
remains until the fresh backend is healthy; SW/index publish last, old hashed
assets remain. Runtime/config/key/Hours/OLD/public+local checks gate completion.

Do not repeat apply after apply-attempt.json exists. The runner's existing
`verify` mode is conditional recovery, NOT read-only: after proving the expected
new backend/process it can finish deferred SW/index and write complete status.
It does not copy backend/restart/restore data or rotate keys. The separately
hash-bound postverify.mjs is read-only with respect to runtime and APIs: only
metadata and20-message latest/warm/older GETs for actual TECH, with source identity
checks and no printed message bodies. It requires completed deployment, never
waits for idle or changes deployment markers. Run its self-test before using it.

Preparation check uses a process-local check-only guard to reject systemError
before the unchanged stock readiness full-history fallback. Never use the guard
for apply. A readiness failure/unknown error is a blocker to inspect, not license
to weaken the gate. Root owns actual activation, existing app Services update
(no duplicate Hours/Files entries), checked Vault publication and final user report.
