# NEW-only rollout boundary — blocked draft

Do not arm this candidate. See ../performance/history-window-2026-09-25.md: giant single native item still unbounded; required checks could not run. No immutable release/manifest/seal has been created and no app availability claim is made.

After implementation and acceptance close, build ONLY /root/WORKTREES/cr-history-window-cache. Compare its emitted modules with the current NEW runtime `/root/RUNNING-SERVICES/codex-remote-secure` and fresh immutable live package. Expected backend allowlist: controller.js/map, http-app.js/map, read-state.js/map, new history-pages.js/map; complete matching client assets/PWA/index. Assert no additional changes, in particular Hours JS/map/template703ad317, push/index, secure protocol modules, generator, native/Vault/session/read receipts/key contents and configuration. Read-state changes only add an accessor; no database migration or reset is authorized.

Reuse the established NEW-only release runner pattern under push-browser-answer-378b37ef as source for a NEW immutable package, never replay its apply. Freeze maintenance imports before publishing. Capture fresh exact source/runtime/process/config baseline without printing secrets; private code-only backups; preserve old hashed assets, publish matching index last. Do not full-copy dist-server or use the stock restart script's OLD-service default. NEW service codex-remote-secure.service, loopback5174; OLD remains disabled. No env change expected.

Root review precedes any arm. ALL NEW turns/queued work/pending requests must be idle, including worker and leader; recheck baseline and idle immediately before mutation with the established invocation. Do not poll one's own active turn. At later completion verify new PID, health, public+local hashes/assets/PWA, cookie-only denial, unchanged identities/Hours, harmless owner access,20-window/cache/pagination behavior; marker complete only after evidence. No production model turn, transcript rewrite, pause/resume or test receipt mutation.

Current artifact hashes cannot be stated before the mandated build. This document is a boundary/runbook, not an executable or reviewed deployment runner.
