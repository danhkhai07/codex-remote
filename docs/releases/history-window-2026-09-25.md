# NEW-only history rollout boundary — not armed

Source candidate only; required checks are blocked. See `../performance/history-window-2026-09-25.md`. No built artifact hashes, release seal or executable activation runner are claimed. Do not reuse a prior apply.

After checks pass, build ONLY `/root/WORKTREES/cr-history-window-cache` and compare against the fresh verified NEW publication `/root/RUNNING-SERVICES/codex-remote-secure`. Expected backend allowlist: `config`, `controller`, `http-app`, `read-state`, new `history-pages`, `history-json`, `rollout-history`, each JS + map; complete matching client/PWA/index. No new npm dependency: built-in node:sqlite requires the installed/pinned Node22.23.2. Verify runtime Node capability through the approved heavy fixture before publication.

The new derived history cache is private0700/0600 at sibling `history-index` beside configured context Vault (NEW expected `/root/.local/state/codex-remote-secure/history-index`); native root comes from the existing CODEX_HOME. No env change, log rewrite, Vault/session/read-state reset or preinstalled private cache snapshot. Cache DBs may be rebuilt from source; never replace native transcripts or restore old user data. Native JSONL compatibility and generated turn-metadata schema are explicit preflight gates. Require the installed-native parity oracle after the bootstrap correction; checkpoint v2 rebuilds only earlier derived indexes. Referenced/ordinal child history is currently an explicit unsupported format, not silent partial success. Do not publish until the target-thread format coverage and pending checks have been reviewed.

Preserve Hours JS `b763a0f7b74c0341684e196855e85b3f5e3b123915a373b27463c17916702e93`, map `6a76da381331d7a802da5d844d341da742f0bfc3809f0cfe3b74370d55c42fb8`, live Hours703ad317 template/generator/data, Push answer implementation, Files owner-full/title, Services middle click, all other backend modules, keys/config/native/Vault/session data. No full dist-server copy. Unexpected emitted module drift requires explanation/review, not adding it silently to the allowlist.

For a later reviewed candidate, adapt the established NEW-only push-browser-answer runner into a NEW immutable release. Capture fresh source/runtime/process/config preimages without printing secrets. Freeze maintenance import closure before backend publication. Preserve old hashed client assets and publish index last. NEW service is `codex-remote-secure.service`, port5174; stock restart script defaults to OLD and must NOT be invoked blindly. OLD remains retired/disabled.

Leader review before arm. ALL NEW turns/queued work/pending requests idle, including own/leader; recheck baseline/idle immediately before copy and restart. Do not poll own busy turn. Backend rollback is code-only and NEW-only at idle; never restore native/user state or regenerate keys. Marker complete only after new PID/health/local+public asset+module hashes, unchanged Hours/key identities, harmless owner access and window/cache smoke evidence. No production model turn, pause/resume or synthetic receipt mutation.

Vault updates require fresh read/merge with `/tmp/history-twenty-cache-vault-draft.md` through NEW env-i knowledge CLI and checked revision. Keep historical investigation as superseded history. Services metadata is a post-publication step, not a claim now.
